description: Limit Switch Binding 

compatible: "limit-switches-for-main-bldc-motor"

properties:
  gpios:
    type: phandle-array
    required: true
    description: GPIO pin phandle for each instance of limit switch. 
